//import * as Actions from "../actions/types.js";

function decks (state = {}, action){
    switch(action.type){
        //cases here
        default :
            return state
    }
}
export default decks;